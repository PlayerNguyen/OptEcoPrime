package com.playernguyen.optecoprime.commands.core;

/**
 * CommandResult enum contains default result for command to response with user
 */
public enum CommandResult {

    
    MISSING_ARGUMENTS, NOTHING
    
}